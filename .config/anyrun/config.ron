Config(
    x: Fraction(0.5),
    y: Fraction(0.3),
    width: Absolute(600),
    height: Absolute(0),
    hide_icons: false,
    ignore_exclusive_zones: false,
    layer: Overlay,
    hide_plugin_info: true,
    close_on_click: true,
    show_results_immediately: true,
    max_entries: Some(8),
    plugins: [
        "libapplications.so",
        "libdictionary.so",
        "librandr.so",
        "librink.so",
        "libshell.so",
        // "libsymbols.so",
        "libtranslate.so",
        "libwebsearch.so",
    ],
)
